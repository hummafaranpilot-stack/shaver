# Backup/temp files
1.zip
*.zip
*.bak
*.tmp

# Sensitive runtime key files (written by UI, not source code)
.smtp_key
.cb_key
.fb_token

# OS files
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/

# FB Traffic Detector — leftover dev artifacts only (data lives in MySQL now)
traffic/logs/
traffic/domains.json
