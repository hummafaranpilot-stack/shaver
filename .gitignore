# Backup/temp files
1.zip
*.zip
*.bak
*.tmp

# Sensitive runtime key files (written by UI, not source code)
.smtp_key
.smtp_host
.smtp_user
.cb_key
.fb_token

# OS files
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/

# FB Traffic Detector — runtime audit logs (visit data itself lives in MySQL)
traffic/logs/blocked_ips.json
traffic/logs/cross_origin.log
traffic/domains.json
